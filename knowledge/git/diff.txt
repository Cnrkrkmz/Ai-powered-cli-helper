# git — Diff and comparison

Unstaged changes: git diff
Staged changes: git diff --cached
Compare branches: git diff main..feature-branch
Compare commits: git diff <hash1> <hash2>
Show only changed file names: git diff --name-only
Statistics summary: git diff --stat
Specific file: git diff -- <file>
